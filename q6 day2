#include <stdio.h>

int main() {
    int m1, m2, m3, m4, m5;

    // Read 5 subject marks
    scanf("%d %d %d %d %d", &m1, &m2, &m3, &m4, &m5);

    // Print each mark on a new line
    printf("%d\n%d\n%d\n%d\n%d", m1, m2, m3, m4, m5);

    return 0;
}